spring:
  application:
    name: oms_usermanagement

  datasource:
    url: jdbc:postgresql://localhost:5439/user_db
    username: postgres
    password: Siphesihle072?
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

jwt:
  secret: OMS_FNB_Super_Secret_Key_2026_Change_This_In_Production
  expiration: 86400000